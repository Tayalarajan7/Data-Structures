Breadth First Search (BFS):
	Breadth First Search of a graph is similar to Breadth First Traversal
of a tree.
	Unlike trees, graphs may contain cycles, so we may come to the same
node again. To avoid processing a node more than once, we use a boolean visited
array.